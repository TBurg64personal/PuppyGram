<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.Burgess.puppygram">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="andriod.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="andriod.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="andriod.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PuppyGram">
        <activity android:name=".NewPicture"></activity>
        <activity android:name=".new_account2"></activity>
        <activity
            android:name=".Login"
            android:label="@string/title_activity_login"
            android:theme="@style/Theme.PuppyGram.NoActionBar"/>
        <activity
            android:name=".ProfilePage"
            android:exported="true" />
        <activity android:name=".NewAccount" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>